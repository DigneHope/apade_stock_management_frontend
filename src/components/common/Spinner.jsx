// components/common/Spinner.jsx
import React from 'react';
import './../assets/styles/loader.css';

const Spinner = () => {
  return <div className="loader"></div>;  // Reuse the loader class you defined earlier in the CSS
};

export default Spinner;
